# 02-架構 — 系統架構與開發規範

本目錄收納**系統怎麼組成、怎麼分層、改的時候有哪些鐵則**。操作步驟不在這裡（看 [06-部署與維護](../06-部署與維護/)），使用者操作更不在這裡（看 [KM](../KM/)）。

| 文件 | 說明 |
| --- | --- |
| [開發慣例與限制速查](開發慣例與限制速查.md) | ⭐ **動手改程式前的唯一入口**：集中列出「改錯就會壞」的鐵則與**違反的後果**，採一句話＋連結，不複製深文件內容。與其他文件衝突時以本檔為準 |
| [解決方案架構](解決方案架構.md) | `CTMS.sln` 各專案的職責與相依方向；⭐ 專案數量的唯一出處 |
| [資料流與分層](資料流與分層.md) | 請求如何在各層流動，以及 CTMS 最關鍵的保存模式——病患 `JsonData` 文件式保存 |
| [技術棧](技術棧.md) | .NET 9 / Blazor Server、三套混用的 UI 套件、EF Core、AI 與影像元件。⚠️ 套件版本以各 `.csproj` 的 `PackageReference` 為準，本文件只記撰寫時的值 |
| [啟動與初始化](啟動與初始化.md) | `Program.cs` 的啟動序列。🔴 **每次啟動自動跑 `Database.Migrate()`**；🔴 授權 middleware 被註解、`[Authorize]` 不會生效；設定檔的 `DefaultConnection`（SQL Server）**沒有被使用** |
| [外部服務串接](外部服務串接.md) | 醫院 LIS／PIP 兩個 WCF 參考、GPT 差異摘要。🔴 「WCF 呼叫完成。」不代表查到資料；🔒 差異摘要會把病患異動 JSON 送到外部服務 |
